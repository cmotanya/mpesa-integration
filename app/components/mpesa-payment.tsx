"use client";

import { Button } from "./button";

export const MPesaPayment = () => {
  return (
    <div>
      <Button
        onClick={() => {}}
        className="bg-primary text-background font-medium uppercase"
      >
        {"Pay with M-PESA"}
      </Button>
    </div>
  );
};
